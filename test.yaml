apiVersion: github.example.com/v1alpha1
kind: File
metadata:
  name: test-file
spec:
  fileContent: |
    apiVersion: v1
    kind: ConfigMap
    metadata:
      name: test-config
    data:
      key: value
  repositoryURL: "nadavshaham/project"
  branch: "master"
  filePath: "configs/test-config.yaml"